# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".

name: auto-approve
on:
  pull_request_target:
    types:
      - labeled
      - opened
      - synchronize
      - reopened
      - ready_for_review
jobs:
  approve:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    if: (github.event.pull_request.user.login == 'pgollucci')
    steps:
      - uses: hmarr/auto-approve-action@v2.1.0
        with:
          github-token: ${{ secrets.P6M7G8_AUTOMATION }}
